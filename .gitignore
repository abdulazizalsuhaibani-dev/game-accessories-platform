# Root-level ignores only.
# Each half keeps its own rules: see backend/.gitignore and frontend/.gitignore.

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Personal Claude Code settings (shared config belongs in .claude/settings.json)
.claude/settings.local.json
